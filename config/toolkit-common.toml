
#ToolKit common config
[playerEvent]
	#if messageOnJoin is true this message will be send to the player on joining world
	message = "§6Welcome to the CipraTech Virtual Voxel World. We promise, we did not teach the cat to press the button. That tuna can? That was lunch. That button with tuna on it? We didn't have plates."
	#Send messages to users on join?
	message_on_join = true

